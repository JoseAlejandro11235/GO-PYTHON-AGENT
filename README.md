# Cursorlite — Python workspace

Small web IDE backed by this Go service, with chat tuned for **Python development** (stdlib, packaging, testing, style). The `workspace/` directory is mounted into the container as your Python project root.
