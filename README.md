# TRUORA-AGENT

Truora agent focused on **Python development**: a small web IDE backed by this Go service, with chat tuned for Python (stdlib, packaging, testing, style). The `workspace/` directory is mounted into the container as your Python project root.