# Copy to .env and fill in secrets (never commit .env).
# Used by docker compose via env_file.

OPENAI_API_KEY=

# Optional: SQLite file for stored agent prompts (default: $WORKSPACE_ROOT/.cursorlite/prompts.db)
# PROMPTS_DB_PATH=
